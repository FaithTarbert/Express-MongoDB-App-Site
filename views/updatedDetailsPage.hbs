<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Attach Accessory</title>
    <link href="/stylesheets/site.css" type="text/css" rel="stylesheet">
  <link rel='shortcut icon' type='image/x-icon' href='../images/favicon.png' />
</head>
<body>
    <div class="container">
        <header>
            <ul>
                <li><img class="logo" src="../images/logo.png"></li>
        <li><a href="/">Browse</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/create">Add a Cube</a></li>
        <li><a href="/create/accessory">Add Accessory</a></li>
        <li><a href="/logout">Logout</a></li>
        <li><a href="/login">Login</a></li>
        <li><a href="/register">Register</a></li>
            </ul>
        </header>
        <main>

      <h1>{{title}}{{cube.name}} </h1>
      <img class="cube" src="{{cube.imageUrl}}">
      <div class="details">
        <p><span>Description:</span> {{cube.description}}</p>
        <p><span>Difficulty level:</span> {{cube.difficulty}}</p>
                <a class="btn" href="/">Back</a>   <a class="btn" href="/attach/{{cube._id}}">Attach</a> <a class="btn" href="/details/update">Update</a>
            </div>
            <h2>Accessories</h2>
            <div class="accessories">
            {{#if cube.accessories}}
              {{#each cube.accessories }}
                 <div class="accessory">
                    <img src="{{imageUrl}}" alt="stickerName">
                    <h3>{{cube.name}}</h3>
                    <p>{{cube.description}}</p>
                </div>
              {{/each}}
              {{else}}
                <h3 class="italic">This cube has no accessories yet...</h3>
              {{/if}}
            </div>
        </main>
    </div>
    <footer>@Cubicle: Exercise for Express.js and Handlebars</footer>
</body>
</html>